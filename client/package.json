{
  "name": "hobolife-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HoboLife — 3D third-person life-sim RPG (game client)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:standalone": "vite build --config vite.standalone.config.ts",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "colyseus.js": "^0.15.26",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
